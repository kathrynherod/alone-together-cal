<h1>Culture Club - Alone Together Prompt Assignments</h1>

{{#each months as |month|}}
    <hr>
    <h2>{{month.name}}</h2>
    <div class="table-responsive">

        <table class="table table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Monday</th>
                    <th scope="col">Tuesday</th>
                    <th scope="col">Wednesday</th>
                    <th scope="col">Thursday</th>
                    <th scope="col">Friday</th>
                </tr>
            </thead>
            <tbody>
                {{#each-in month.weeks as |week days index|}}
                    <tr>
                        {{#if (eq week month.firstWeek)}}
                            {{#if (eq days.length 1)}}
                                {{#each days as |day|}}
                                    <td></td>
                                    <td></td>
                                    <td></td>
                                    <td></td>
                                    <td>
                                        <div class="date">
                                            {{day.date}}
                                        </div>
                                        <div class="poster">
                                            {{day.poster}}
                                        </div>
                                    </td>
                                {{/each}}
                            {{else if (eq days.length 2)}}
                                <td></td>
                                <td></td>
                                {{#each days as |day index|}}
                                    {{#if (eq index 0)}}
                                        <td>
                                            <div class="date">
                                                {{day.date}}
                                            </div>
                                            <div class="poster">
                                                {{day.poster}}
                                            </div>
                                        </td>
                                        <td></td>
                                    {{else}}
                                        <td>
                                            <div class="date">
                                                {{day.date}}
                                            </div>
                                            <div class="poster">
                                                {{day.poster}}
                                            </div>
                                        </td>
                                    {{/if}}
                                {{/each}}
                            {{else}}
                                {{#each days as |day index|}}
                                    {{#if (eq index 1)}}
                                        <td></td>
                                        <td>
                                            <div class="date">
                                                {{day.date}}
                                            </div>
                                            <div class="poster">
                                                {{day.poster}}
                                            </div>
                                        </td>
                                        <td></td>
                                    {{else}}
                                        <td>
                                            <div class="date">
                                                {{day.date}}
                                            </div>
                                            <div class="poster">
                                                {{day.poster}}
                                            </div>
                                        </td>
                                    {{/if}}
                                {{/each}}
                            {{/if}}
                        {{else}}
                            {{#if (eq days.length 1)}}
                                {{#each days as |day|}}
                                    <td></td>
                                    <td></td>
                                    <td></td>
                                    <td></td>
                                    <td>
                                        <div class="date">
                                            {{day.date}}
                                        </div>
                                        <div class="poster">
                                            {{day.poster}}
                                        </div>
                                    </td>
                                {{/each}}
                            {{else if (eq days.length 2)}}
                                <td></td>
                                <td></td>
                                {{#each days as |day index|}}
                                    {{#if (eq index 0)}}
                                        <td>
                                            <div class="date">
                                                {{day.date}}
                                            </div>
                                            <div class="poster">
                                                {{day.poster}}
                                            </div>
                                        </td>
                                        <td></td>
                                    {{else}}
                                        <td>
                                            <div class="date">
                                                {{day.date}}
                                            </div>
                                            <div class="poster">
                                                {{day.poster}}
                                            </div>
                                        </td>
                                    {{/if}}
                                {{/each}}
                            {{else}}
                                {{#each days as |day index|}}
                                    {{#if (eq index 1)}}
                                        <td></td>
                                        <td>
                                            <div class="date">
                                                {{day.date}}
                                            </div>
                                            <div class="poster">
                                                {{day.poster}}
                                            </div>
                                        </td>
                                        <td></td>
                                    {{else}}
                                        <td>
                                            <div class="date">
                                                {{day.date}}
                                            </div>
                                            <div class="poster">
                                                {{day.poster}}
                                            </div>
                                        </td>
                                    {{/if}}
                                {{/each}}
                            {{/if}}
                        {{/if}}
                    </tr>
                {{/each-in}}
            </tbody>
        </table>
    </div>

{{/each}}
